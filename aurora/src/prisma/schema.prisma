generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  //url      = env("DATABASE_URL")
  url= "mongodb+srv://next:htE4ALg3jKZh3rZZ@aurora.p3jx0.mongodb.net/Aurora/?retryWrites=true&w=majority"
  // url= "mongodb://localhost:27017/aurora"
}

model Project{
  id            String      @id @default(auto()) @map("_id") @db.ObjectId
  title         String      @unique
  description   String?
  stack String[]
}
