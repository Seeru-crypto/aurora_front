generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = "mongodb+srv://next_js:VzqsSNc2yEBYs0aR@aurora.p3jx0.mongodb.net/Aurora/"
}

model Project{
  id            String      @id @default(auto()) @map("_id") @db.ObjectId
  title         String      @unique
  description   String?
  stack String[]
}
