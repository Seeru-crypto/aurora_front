generator client {
  provider      = "prisma-client-js"
  binaryTargets = ["native", "darwin"]
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

model aurora_collection {
  id String @id @default(auto()) @map("_id") @db.ObjectId
}
